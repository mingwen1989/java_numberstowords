apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "numbers-words"
version = '1.0'
mainClassName = "NumbersWords"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
